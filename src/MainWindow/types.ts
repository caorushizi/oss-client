export enum Platform {
  windows = "win32",
  macos = "darwin",
  linux = "linux"
}

export enum Direction {
  up,
  down
}

export enum Layout {
  grid,
  table
}

// fixme: 首字母
export enum Page {
  bucket,
  transferList,
  transferDone,
  setting,
  apps
}
