export enum Platform {
  windows = "win32",
  macos = "darwin",
  linux = "linux"
}

export enum Direction {
  up = "up",
  down = "down"
}

export enum Layout {
  grid,
  table
}

export enum Page {
  bucket,
  transferList,
  transferDone,
  setting,
  apps
}

export enum KeyCode {
  Escape = "Escape"
}
