export enum OssType {
  qiniu,
  ali,
  tencent,
}

export enum TaskType {
  download,
  upload,
}

export enum TransferStatus {
  default,
  done,
  failed,
}

export enum Theme {
  simple,
  colorful,
}

export enum FlowWindowStyle {
  circle,
  oval,
}
