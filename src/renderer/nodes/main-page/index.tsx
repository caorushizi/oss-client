import React, { FC } from "react";
import { Button } from "@chakra-ui/react";

const MainPage: FC = () => {
  return (
    <div>
      <Button>你好世界</Button>
    </div>
  );
};

export default MainPage;
